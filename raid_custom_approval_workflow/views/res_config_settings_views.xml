<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.raid.approval</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="90"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <!-- Sales Settings -->
            <xpath expr="//form//app[@name='sale_management']" position="inside">
                <block title="Raid Sales Approvals" id="raid_sale_approval_setting">
                    <setting string="Sales Approval Cycle"
                             help="Choose which approval cycle (if any) applies to Sales Orders for the current company.">
                        <field name="sale_approval_cycle_type" widget="radio"/>
                    </setting>
                </block>
            </xpath>
            <!-- Purchase Settings -->
            <xpath expr="//form//app[@name='purchase']" position="inside">
                <block title="Raid Purchase Approvals" id="raid_purchase_approval_setting">
                    <setting string="Purchase Approval Cycle"
                             help="Choose which approval cycle (if any) applies to Purchase Orders for the current company.">
                        <field name="purchase_approval_cycle_type" widget="radio"/>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
