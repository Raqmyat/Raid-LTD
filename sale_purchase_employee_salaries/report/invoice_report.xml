<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- نفس المنطق بالظبط لكن على قالب فاتورة أودو الأساسي - كله بالإنجليزي. -->
    <template id="report_invoice_document_employee_note" inherit_id="account.report_invoice_document">
        <xpath expr="//span[@t-field='line.name']" position="after">
            <div t-if="line.employee_id" class="text-muted" style="font-size: 0.85em;">
                Employee: <span t-field="line.employee_id.name"/>
            </div>
        </xpath>

        <xpath expr="//div[hasclass('page')]" position="inside">
            <div t-if="doc.is_salaries and doc.payslip_run_id" class="mt-4">
                <h4>Salary Details</h4>
                <t t-set="matrix" t-value="doc._get_salary_matrix()"/>
                <table class="table table-sm table-bordered">
                    <thead>
                        <tr>
                            <th>Employee</th>
                            <t t-foreach="matrix['columns']" t-as="col">
                                <th><t t-esc="col['name']"/></th>
                            </t>
                            <th>Total Employee Cost</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr t-foreach="matrix['rows']" t-as="row">
                            <td><t t-esc="row['employee']"/></td>
                            <t t-foreach="row['values']" t-as="val">
                                <td class="text-end"><t t-esc="'%.2f' % val"/></td>
                            </t>
                            <td class="text-end"><strong><t t-esc="'%.2f' % row['employee_cost']"/></strong></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </xpath>
    </template>
</odoo>
